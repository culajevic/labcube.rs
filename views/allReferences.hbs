{{> _innerNavBackend}}
<div class="container">
  <div class="row">
    {{#if numOfReferences}}
      <table class="table table-hover mt-4">
        <!-- <thead class="d-flex">
          <!-- <th class=""> -->
            <!-- <td class="col-8">Referenca</th> -->
          </th>
        </thead>
        <tbody class="mt-4">
          {{#each allReferences}}
          <tr class="d-flex">
            <td class="col-10">{{this.referenceTitle}}</td>
            <!-- <td class="col-2">{{this.referenceLink}}</td> -->
            <td class="col-1"><a href='addReference/{{this._id}}' class="btn btn-info">Edit</a></td>
            <td class="col-1"><a href='' data-id={{this.id}} class="btn btn-danger deleteDocument">Delete</a></td>
          </tr>
          {{/each}}
        </tbody>
      </table>
      {{else}}
        <div class="m-auto">
          <h3 class="mt-4">Trenutno nema nijedna referenca u bazi</h3>
        </div>
      {{/if}}
  </div>
</div>

{{> _footer}}
