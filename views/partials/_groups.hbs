<section id="analize-wrapper">
    <div class="container">
      <div class="row analize-container">
{{#each groups }}
      <div class="col-12 col-md-6 col-lg-4 d-flex align-self-stretch">
        <div class="card">
          <div class="card-body">
            <div>
              <img src={{this.iconPath}} alt="{{this.name}}" class="analiza-ikonica">
              <span class="d-flex align-self-stretch">{{this.name}}</span>
            </div>
            <p class="card-text">{{groupSummary this.description}}</p>
            <a href='details/{{this.name}}'><button class="button btn btn-secondary btn-block click-more">Saznaj više
            <span class="broj-analiza">25</span></button></a>
            </div><!-- card body end -->
          </div><!-- card end -->
        </div><!-- group div end -->
{{/each}}
        </div>
        <div class="text-center">
          <button class="btn btn-success text-uppercase">sve analize</button>
        </div>
    </div><!-- container end -->
  </section><!-- analize-wrapper end -->
